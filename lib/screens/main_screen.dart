import 'package:flutter/material.dart';

class Bleeper_Widjet extends StatelessWidget {
  const Bleeper_Widjet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
